<views:MvxWpfView x:Class="Peernet.Browser.WPF.Views.FiltersView"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf">

    <Border Width="700" BorderThickness="0" Padding="40" BorderBrush="Transparent" CornerRadius="{StaticResource MainRadius}">
        <Border.Background>
            <SolidColorBrush Color="{StaticResource MainBackground}" />
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0">
                <TextBlock FontWeight="DemiBold" FontSize="16">Your filters</TextBlock>
                <Button Margin="15 0" VerticalAlignment="Center" Style="{StaticResource LinkButton}">Clear all</Button>
            </DockPanel>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Grid.RowSpan="2">
                    <TextBlock Margin="0 15 0 8">DATE</TextBlock>

                    <Border Padding="15 10" BorderBrush="{StaticResource LightGrayBrush}" BorderThickness="0.5" 
                            CornerRadius="{StaticResource MainRadius}">
                        <StackPanel>
                            <CheckBox Margin="0 10" Template="{StaticResource RoundedCheckBox}" VerticalContentAlignment="Center">Any time</CheckBox>
                            <CheckBox Margin="0 10" Template="{StaticResource RoundedCheckBox}" VerticalContentAlignment="Center">Last 24 hours</CheckBox>
                            <CheckBox Margin="0 10" Template="{StaticResource RoundedCheckBox}" VerticalContentAlignment="Center">Last Week</CheckBox>
                            <CheckBox Margin="0 10" Template="{StaticResource RoundedCheckBox}" VerticalContentAlignment="Center">Last 30 Days</CheckBox>
                            <CheckBox Margin="0 10" Template="{StaticResource RoundedCheckBox}" VerticalContentAlignment="Center">Last Month</CheckBox>
                            <CheckBox Margin="0 10" Template="{StaticResource RoundedCheckBox}" VerticalContentAlignment="Center">Last Year</CheckBox>
                            <CheckBox Margin="0 10" Template="{StaticResource RoundedCheckBox}" VerticalContentAlignment="Center">Custom</CheckBox>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <DockPanel HorizontalAlignment="Right" Grid.Row="2">
                <Button Height="30" Width="110" Style="{StaticResource PrimaryButton}">Cancel</Button>
                <Button Height="30" Width="110" Style="{StaticResource AcceptButton}">Apply filters</Button>
            </DockPanel>
        </Grid>
    </Border>
</views:MvxWpfView>