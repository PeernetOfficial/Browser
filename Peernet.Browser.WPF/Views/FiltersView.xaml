<views:MvxWpfView x:Class="Peernet.Browser.WPF.Views.FiltersView"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
                  xmlns:controls="clr-namespace:Peernet.Browser.WPF.Controls">

    <Border BorderThickness="0" Padding="40" BorderBrush="Transparent" CornerRadius="{StaticResource MainRadius}">
        <Border.Background>
            <SolidColorBrush Color="{StaticResource MainBackground}" />
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0">
                <TextBlock FontWeight="DemiBold" FontSize="16">Your filters</TextBlock>
                <Button Margin="15 0" VerticalAlignment="Center" Style="{StaticResource LinkButton}">Clear all</Button>
            </DockPanel>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <controls:CustomFilterControl Grid.Column="0" Grid.RowSpan="2" Margin="0 0 15 0" DataContext="{Binding DateFilters}" />
                <controls:CustomFilterControl Grid.Column="1" Grid.RowSpan="2" Margin="0 0 15 0" DataContext="{Binding FileFormatFilters}" />
                <controls:CustomFilterControl Grid.Column="3" Grid.RowSpan="2" Margin="15 0 0 0" DataContext="{Binding SortOrderFilters}" />

                <StackPanel Grid.Row="0" Grid.Column="2">
                    <TextBlock Margin="0 15 0 8" Text="HEALTH" />
                    <Border Padding="15 10"  BorderBrush="{StaticResource LightGrayBrush}" BorderThickness="0.5" CornerRadius="{StaticResource MainRadius}">
                        <StackPanel>
                            <CheckBox Foreground="{StaticResource DarkGreenBrush}" Margin="0 10" Template="{StaticResource RoundedCheckBox}"
                                  VerticalContentAlignment="Center">
                                <DockPanel>
                                    <Canvas Width="7" Height="7" Margin="0 0 5 0">
                                        <Ellipse Fill="#13BB1A" Width="7" Height="7" />
                                    </Canvas>
                                    <TextBlock>Grean health indicator</TextBlock>
                                </DockPanel>
                            </CheckBox>

                            <CheckBox Foreground="{StaticResource DarkGreenBrush}" Margin="0 10" Template="{StaticResource RoundedCheckBox}"
                                  VerticalContentAlignment="Center">
                                <DockPanel>
                                    <Canvas Width="7" Height="7" Margin="0 0 5 0">
                                        <Ellipse Fill="#F8C537" Width="7" Height="7" />
                                    </Canvas>
                                    <TextBlock>Yellow health indicator</TextBlock>
                                </DockPanel>
                            </CheckBox>

                            <CheckBox Foreground="{StaticResource DarkGreenBrush}" Margin="0 10" Template="{StaticResource RoundedCheckBox}"
                                  VerticalContentAlignment="Center">
                                <DockPanel>
                                    <Canvas Width="7" Height="7" Margin="0 0 5 0">
                                        <Ellipse Fill="#C50000" Width="7" Height="7" />
                                    </Canvas>
                                    <TextBlock>Red health indicator</TextBlock>
                                </DockPanel>
                            </CheckBox>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="2">
                    <TextBlock Margin="0 15 0 8" Text="SIZE" />
                    <Border BorderBrush="{StaticResource LightGrayBrush}" BorderThickness="0.5" CornerRadius="{StaticResource MainRadius}">
                        <controls:MyRangeSlider  Maximum="100" Minimum="0" RangeMax="80"  RangeMin="20" />
                    </Border>
                </StackPanel>
            </Grid>

            <DockPanel HorizontalAlignment="Right" Grid.Row="2">
                <Button Height="30" Width="110" Style="{StaticResource PrimaryButton}">Cancel</Button>
                <Button Height="30" Width="110" Style="{StaticResource AcceptButton}">Apply filters</Button>
            </DockPanel>
        </Grid>
    </Border>
</views:MvxWpfView>