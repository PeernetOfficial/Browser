<views:MvxWpfView
    x:Class="Peernet.Browser.WPF.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf">
    <Grid>
        <StackPanel>
            <Image Source="/Assets/Logo.png" Width="278.04" Height="66" Margin="0,0,0,90" />
            <Border BorderBrush="White" BorderThickness="0" CornerRadius="3,3,3,3">
                <Border.Background>
                    <SolidColorBrush Color="White"></SolidColorBrush>
                </Border.Background>
                <Grid Width="612" Height="44">
                    <TextBlock Width="80" Padding="15" TextBlock.FontSize="14"
                                     TextAlignment="Center"
                                     VerticalAlignment="Center"
                                     FontFamily="Inter"
                                     Background="Transparent"
                                     Foreground="{StaticResource LightGray2Brush}" Text="Search files">
                        <TextBlock.Visibility>
                            <MultiBinding Converter="{StaticResource TextInputToVisibilityConverter}">
                                <Binding ElementName="txtSearch" Path="Text.IsEmpty" />
                                <Binding ElementName="txtSearch" Path="IsFocused" />
                            </MultiBinding>
                        </TextBlock.Visibility>
                    </TextBlock>

                    <TextBox Width="575"
                                     Margin="5,0,30,0"
                                     Text="{Binding SearchInput, UpdateSourceTrigger=PropertyChanged}"
                                     VerticalAlignment="Center"
                                     TextBlock.FontSize="14"
                                     Foreground="Black"
                                     Background="Transparent"
                                     BorderThickness="0" Name="txtSearch">
                    </TextBox>

                    <Image Margin="570,0,0,0" Source="/Assets/Search.png" Height="14.22" Width="14.22">
                        <Image.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding Search}" />
                        </Image.InputBindings>
                    </Image>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</views:MvxWpfView>