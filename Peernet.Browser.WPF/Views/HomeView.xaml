<views:MvxWpfView
    x:Class="Peernet.Browser.WPF.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf">
    <Grid VerticalAlignment="Center">
        <StackPanel Width="600">
            <Image Source="/Assets/Logo.png" Width="278.04" Height="66" Margin="0,0,0,90" />
            <Border Padding="15" BorderBrush="White" BorderThickness="0" CornerRadius="{StaticResource MainRadius}">
                <Border.Background>
                    <SolidColorBrush Color="White"></SolidColorBrush>
                </Border.Background>
                <Grid>
                    <TextBlock TextBlock.FontSize="14"
                                     TextAlignment="Center"
                                     VerticalAlignment="Center"
                                     FontFamily="Inter"
                                     Background="Transparent"
                                     Foreground="{StaticResource LightGray2Brush}" Text="Search files">
                        <TextBlock.Visibility>
                            <MultiBinding Converter="{StaticResource TextInputToVisibilityConverter}">
                                <Binding ElementName="txtSearch" Path="Text.IsEmpty" />
                                <Binding ElementName="txtSearch" Path="IsFocused" />
                            </MultiBinding>
                        </TextBlock.Visibility>
                    </TextBlock>

                    <TextBox Text="{Binding SearchInput, UpdateSourceTrigger=PropertyChanged}"
                                     VerticalAlignment="Center"
                                     TextBlock.FontSize="14"
                                     Foreground="Black"
                                     Background="Transparent"
                                     BorderThickness="0" Name="txtSearch">
                        <TextBox.InputBindings>
                            <KeyBinding Command="{Binding Search}" Key="Enter" />
                        </TextBox.InputBindings>
                    </TextBox>

                    <Image HorizontalAlignment="Right" Source="/Assets/Search.png" Height="14.22" Width="14.22">
                        <Image.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding Search}" />
                        </Image.InputBindings>
                    </Image>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</views:MvxWpfView>