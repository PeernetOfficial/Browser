<views:MvxWpfView
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    xmlns:mvx="clr-namespace:MvvmCross.Platforms.Wpf.Binding;assembly=MvvmCross.Platforms.Wpf"
    x:Class="Peernet.Browser.WPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:Peernet.Browser.WPF.Controls" 
             xmlns:local="clr-namespace:Peernet.Browser.WPF.Views" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600"
             >
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/AppBase.png" />
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:NavigationBar Grid.Row="0" DataContext="{Binding NavigationBarViewModel}" />
        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height = "*" />
                <RowDefinition Height = "20" />
                <RowDefinition Height = "*" />
            </Grid.RowDefinitions>

            <Image Grid.Row="0" Grid.Column="3" Source="/Assets/Logo.png" Width="274" Height="30"/>

            <Border Grid.Row="1" Grid.Column="3" BorderBrush="White" BorderThickness="1" CornerRadius="3,3,3,3">
                <Border.Background>
                    <SolidColorBrush Color="White" Opacity="0.06"></SolidColorBrush>
                </Border.Background>
                <DockPanel>
                    <TextBox DockPanel.Dock="Left" 
                         Height="Auto" 
                         mvx:Bi.nd="Text SearchInput" 
                         Width="300"
                         Background="Transparent"
                         Foreground="White"
                         BorderThickness="0" >
                        <TextBox.InputBindings>
                            <KeyBinding Command="{Binding Search}" Key="Enter" />
                        </TextBox.InputBindings>
                    </TextBox>
                    <Image DockPanel.Dock="Right" Source="/Assets/Search.png" Height="Auto" Width="20" />
                </DockPanel>
            </Border>
        </Grid>
        <controls:Footer Grid.Row="2" DataContext="{Binding FooterViewModel}" />
    </Grid>

</views:MvxWpfView>
