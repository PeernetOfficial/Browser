<views:MvxWpfView
    x:Class="Peernet.Browser.WPF.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf">
    <Grid VerticalAlignment="{Binding Alignment, Converter={StaticResource VerticalAlignmentConverter}}">
        <Image HorizontalAlignment="Left" VerticalAlignment="Top" Margin="60 0" Source="{StaticResource LogoAbbreviationDrawingImage}" Height="40" Visibility="{Binding IsVisible, Converter={StaticResource BoolToVisibility}}" />
        <StackPanel Width="500">
            <Image Source="{StaticResource LogoDrawingImage}" Height="55" Visibility="{Binding IsNotVisible, Converter={StaticResource BoolToVisibility}}" />
            <Border Padding="15" BorderBrush="White" BorderThickness="0" Margin="0 90 0 0" CornerRadius="{StaticResource MainRadius}">
                <Border.Background>
                    <SolidColorBrush Color="White"></SolidColorBrush>
                </Border.Background>
                <Grid>
                    <TextBlock TextBlock.FontSize="14"
                                     TextAlignment="Center"
                                     VerticalAlignment="Center"
                                     FontFamily="Inter"
                                     Background="Transparent"
                                     Foreground="{StaticResource LightGray2Brush}" Text="Search files">
                        <TextBlock.Visibility>
                            <MultiBinding Converter="{StaticResource TextInputToVisibilityConverter}">
                                <Binding ElementName="txtSearch" Path="Text.IsEmpty" />
                                <Binding ElementName="txtSearch" Path="IsFocused" />
                            </MultiBinding>
                        </TextBlock.Visibility>
                    </TextBlock>

                    <TextBox Text="{Binding SearchInput, UpdateSourceTrigger=PropertyChanged}"
                                     VerticalAlignment="Center"
                                     TextBlock.FontSize="14"
                                     Foreground="Black"
                                     Background="Transparent"
                                     BorderThickness="0" Name="txtSearch">
                        <TextBox.InputBindings>
                            <KeyBinding Command="{Binding SearchCommand}" Key="Enter" />
                        </TextBox.InputBindings>
                    </TextBox>

                    <Image HorizontalAlignment="Right" Source="{StaticResource SearchDrawingImage}" Height="14.22" Width="14.22">
                        <Image.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding SearchCommand}" />
                        </Image.InputBindings>
                    </Image>
                </Grid>
            </Border>
        </StackPanel>

        <TabControl Visibility="{Binding IsVisible, Converter={StaticResource BoolToVisibility}}" SelectedIndex="{Binding SelectedIndex}"
                    Foreground="{StaticResource DarkGreenBrush}" Margin="0 180 0 0" ItemsSource="{Binding Tabs}" />
    </Grid>
</views:MvxWpfView>