<views:MvxWpfView
    x:Class="Peernet.Browser.WPF.Views.DictionaryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    xmlns:models="clr-namespace:Peernet.Browser.Application.Models;assembly=Peernet.Browser.Application"
    xmlns:controls="clr-namespace:Peernet.Browser.WPF.Controls"
    xmlns:extensions="clr-namespace:Peernet.Browser.WPF.Extensions"
    Height="600" Width="1441"
    x:Name="DirectoryView">
    <Border Background="White" BorderThickness="0" CornerRadius="8,8,8,8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Width="233" HorizontalAlignment="Center" Margin="0,28,0,0">
                <StackPanel.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type models:VirtualFileSystemEntity}" ItemsSource="{Binding Path=VirtualFileSystemTiers}">
                        <StackPanel Orientation="Horizontal">
                            <Image Width="11" Height="10" Source="/Assets/Directory_GrayedOut.png" />
                            <TextBlock x:Name="PART_EditableTextBox" Text="{Binding Path=Name}" Margin="7, 0, 0, 0">
                                <TextBlock.InputBindings>
                                    <MouseBinding Command="{Binding DataContext.UpdateActiveSearchResults, ElementName=DirectoryView}" CommandParameter="{Binding}" Gesture="LeftDoubleClick"/>
                                </TextBlock.InputBindings>
                            </TextBlock>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type models:VirtualFileSystemCategory}" ItemsSource="{Binding Path=VirtualFileSystemCategories}">
                        <StackPanel Orientation="Horizontal">
                            <Image Width="11" Height="10" Source="/Assets/Directory_GrayedOut.png" />
                            <TextBlock Text="{Binding Path=Name}" Margin="7, 0, 0, 0">
                                <TextBlock.InputBindings>
                                    <MouseBinding  Command="{Binding DataContext.UpdateActiveSearchResults, ElementName=DirectoryView}" CommandParameter="{Binding}" Gesture="LeftDoubleClick"/>
                                </TextBlock.InputBindings>
                            </TextBlock>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </StackPanel.Resources>
                <TreeView BorderThickness="0" HorizontalAlignment="Center" >
                    <TreeViewItem Template="{DynamicResource TreeViewItemTemplate}" ItemsSource="{Binding VirtualFileSystem.VirtualFileSystemTiers}" Header="Your Files" Foreground="{StaticResource DarkGreenBrush}" />
                    <TreeViewItem Template="{DynamicResource TreeViewItemTemplate}" ItemsSource="{Binding VirtualFileSystem.VirtualFileSystemCategories}" Header="Libraries" Foreground="{StaticResource DarkGreenBrush}" />
                </TreeView>
            </StackPanel>
            <Separator Style="{StaticResource StandardSeparatorStyle}" Grid.Column="1" Width="500" RenderTransformOrigin="0.5,0.5">
                <Separator.RenderTransform>
                    <TransformGroup>
                        <RotateTransform Angle="90" />
                    </TransformGroup>
                </Separator.RenderTransform>
            </Separator>
            <Grid Grid.Column="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Margin="63, 40, 0, 0">Path</TextBlock>
                <Border Grid.Row="0" Grid.Column="1" Width="229" Height="40" HorizontalAlignment="Right" Margin="0,28, 85, 27" BorderThickness="1" CornerRadius="3,3,3,3">
                    <Border.BorderBrush>
                        <SolidColorBrush Opacity="0.06" Color="Black" />
                    </Border.BorderBrush>
                    <Grid>
                        <TextBlock Width="80" Padding="15" FontSize="14" Visibility="{Binding ShowHint, Converter={StaticResource BoolToVisibility}}"
                                   TextAlignment="Center"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Left"
                                   FontFamily="Inter"
                                   Background="Transparent"
                                   Foreground="#B0B0B0" Text="Search">
                        </TextBlock>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox Margin="20,0,30,0"
                                     Text="{Binding SearchInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     VerticalAlignment="Center"
                                     TextBlock.FontSize="14"
                                     Foreground="Black"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     extensions:FocusExtension.IsFocused="{Binding ShowSearchBox}">
                                <TextBox.InputBindings>
                                    <KeyBinding Command="{Binding Search}" Key="Enter" />
                                    <MouseBinding MouseAction="LeftClick" Command="{Binding RemoveHint}" />
                                </TextBox.InputBindings>
                            </TextBox>

                            <Image Grid.Column="1" Source="/Assets/Search_Light.png" Height="14.22" Width="14.22" HorizontalAlignment="Right" Margin="0,0,20,0">
                                <Image.InputBindings>
                                    <MouseBinding MouseAction="LeftClick" Command="{Binding Search}" />
                                </Image.InputBindings>
                            </Image>
                        </Grid>
                    </Grid>
                </Border>
                <controls:SharedFilesDataGridControl  Grid.Row="1" Grid.ColumnSpan="2" Margin="37,0,0,0" />
            </Grid>
        </Grid>
    </Border>
</views:MvxWpfView>