<UserControl
    x:Class="Peernet.Browser.WPF.Controls.ProfileMenuControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:Peernet.Browser.WPF.Controls"
    mc:Ignorable="d"
    d:DesignHeight="40"
    d:DesignWidth="1441">

    <Border BorderBrush="White" BorderThickness="0" Width="212">
        <Border.Background>
            <SolidColorBrush Color="{DynamicResource ModalBackground}" />
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect ShadowDepth="3" BlurRadius="16" Color="Black" Opacity="0.14" />
        </Border.Effect>
        <StackPanel>
            <Grid Margin="17,18,0,17">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0" Source="{Binding UserContext.User.Image}" Width="47" Height="47" />
                <StackPanel Grid.Column="1" Margin="8,0,0,0" Orientation="Horizontal">
                    <TextBlock Foreground="{DynamicResource MainContentBrush}" FontFamily="Inter" FontSize="14" FontWeight="Medium" Text="{Binding UserContext.User.Name}" />
                    <Button Style="{StaticResource ButtonBaseStyle}" VerticalAlignment="Top" HorizontalAlignment="Left" Width="16" Height="16" Margin="31,0,0,0" Command="{Binding EditProfileCommand}">
                        <Button.Background>
                            <ImageBrush ImageSource="{DynamicResource EditDrawingImage}" />
                        </Button.Background>
                    </Button>
                </StackPanel>
            </Grid>
            <Separator Style="{StaticResource StandardSeparatorStyle}" Width="179" />
            <Button Command="{Binding NavigateAboutCommand}" Content="About" Style="{StaticResource NavbarMenuButtonStyle}" />
            <Button Content="Settings" Style="{StaticResource NavbarMenuButtonStyle}" />
            <Separator Style="{StaticResource StandardSeparatorStyle}" Width="179" />
            <StackPanel Orientation="Horizontal" Margin="17, 26, 0, 21">
                <TextBlock Text="Light" Foreground="{DynamicResource MainTextBrush}" />
                <controls:ToggleSwitch Margin="13, 0, 13, 0" />
                <TextBlock Text="Dark" Foreground="{DynamicResource MainTextBrush}" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>