<UserControl x:Class="Peernet.Browser.WPF.Controls.MasterDataControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:contexts="clr-namespace:Peernet.Browser.Application.Contexts;assembly=Application"
             xmlns:models="clr-namespace:Peernet.SDK.Models.Presentation;assembly=Peernet.SDK"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ImageBrush x:Key="DefaultProfileImage" ImageSource="{DynamicResource DefaultProfileImageDrawingImage}" />
            <ImageBrush x:Key="SourcedProfileImage" ImageSource="{Binding User.Image, Converter={StaticResource NullImageConverter}}" Stretch="Uniform" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="{DynamicResource ViewBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Grid.Column="0" Margin="35,35,27,0" HorizontalAlignment="Left">
            <Border Background="{StaticResource LightGray2Brush}" Width="200" Height="200" Margin="0,8,0,0">
                <Border>
                    <Border.Style>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="Background" Value="{StaticResource SourcedProfileImage}" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding User.Image}" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource DefaultProfileImage}" />
                                    <Setter Property="Width" Value="36" />
                                    <Setter Property="Height" Value="36" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                </Border>
            </Border>
            <Border Width="282" Background="Transparent" BorderThickness="0" Margin="0,32,0,0" HorizontalAlignment="Center">
                <TextBlock Text="{Binding User.Name}" FontSize="20" Foreground="{DynamicResource MainContentBrush}" FontWeight="Medium" />
            </Border>

            <StackPanel Orientation="Horizontal">
                <TextBlock Style="{StaticResource Underlined}" Text="68 Following" Padding="0, 5, 5, 5" />
                <TextBlock Style="{StaticResource Underlined}" Text="32 Followers" Padding="5" />
            </StackPanel>

            <Border Width="282" Height="36" CornerRadius="0,0,3,3" Margin="0,12,0,0" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" HorizontalAlignment="Center">
                <Border.Style>
                    <Style TargetType="{x:Type Border}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Path=(contexts:GlobalContext.VisualMode)}" Value="{x:Static models:VisualMode.DarkMode}">
                                <Setter Property="Background" Value="Transparent" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Report account" VerticalAlignment="Center" FontSize="14" Background="Transparent">
                        <TextBlock.Style>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="Foreground" Value="{DynamicResource MainContentBrush}" />
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <Border Grid.Column="1" Background="Transparent" Padding="10" Cursor="Hand">
                        <TextBlock Text="Report" Style="{DynamicResource Underlined}" Foreground="#E10D0D" VerticalAlignment="Center" />
                    </Border>
                </Grid>
            </Border>
        </StackPanel>
        <Border Grid.Row="1" Grid.Column="1" Width="1" BorderThickness="0">
            <Border.Background>
                <SolidColorBrush Color="{DynamicResource MainTextColor}" Opacity="0.1" />
            </Border.Background>
        </Border>

        <Border Grid.Row="1" Grid.Column="2" Margin="20" Padding="1">
            <Border.Background>
                <SolidColorBrush Color="{DynamicResource MainTextColor}" Opacity="0.1" />
            </Border.Background>
            <TextBox TextWrapping="Wrap" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <TextBox.Style>
                    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource ReadonlySelectableTextBox}">
                        <Setter Property="Text" Value="{Binding User.Description}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding User.Description}" Value="{x:Null}">
                                <Setter Property="Text" Value="The user has no description." />
                                <Setter Property="Foreground" Value="{DynamicResource LightGray2Brush}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding User.Description}" Value="">
                                <Setter Property="Text" Value="The user has no description." />
                                <Setter Property="Foreground" Value="{DynamicResource LightGray2Brush}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Border>
    </Grid>
</UserControl>