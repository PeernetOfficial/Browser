<views:MvxWindow  x:Class="Peernet.Browser.WPF.FilePreviewWindow"
                  x:TypeArguments="viewModels:FilePreviewViewModel"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
                  xmlns:viewModels="clr-namespace:Peernet.Browser.Application.ViewModels;assembly=Application"
                  Width="700"
                  Height="500"
                  Top="100"
                  Left="100"
                  x:Name="PreviewWindow"
                  Background="{DynamicResource PreviewBackground}" Content="{Binding}">
    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="0" GlassFrameThickness="0" CaptionHeight="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <views:MvxWindow.Resources>
        <ResourceDictionary>
            <Style x:Key="ReadonlySelectableTextBox" TargetType="{x:Type TextBox}">
                <Setter Property="Foreground" Value="{DynamicResource MainContentBrush}" />
                <Setter Property="FontFamily" Value="{StaticResource InterThin}" />
                <Setter Property="IsReadOnly" Value="True" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Background" Value="Transparent" />
            </Style>
        </ResourceDictionary>
    </views:MvxWindow.Resources>

        <views:MvxWindow.ContentTemplate>
        <DataTemplate>
                <Border BorderThickness="1">
                <Border.BorderBrush>
                    <SolidColorBrush Opacity="0.2" Color="Black" />
                </Border.BorderBrush>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Style="{StaticResource CloseButtonStyle}" Click="Close_OnClick" />
                        </StackPanel>
                    <StackPanel Grid.Row="1" Margin="40, 0, 0, 0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Information" Style="{StaticResource Active}" FontSize="32" FontWeight="Medium" />
                            <Image Source="{StaticResource ShareDrawingImage}" Margin="380, 0, 0, 0">
                                <Image.Style>
                                    <Style TargetType="{x:Type Image}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=IsEditable}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <Image Source="{StaticResource BinDrawingImage}" Margin="26, 0, 0, 0">
                                <Image.Style>
                                    <Style TargetType="{x:Type Image}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=IsEditable}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0, 24, 0, 0">
                            <Image Style="{StaticResource ItemImageStyle}" Margin="0,0,14,0" />
                            <TextBox Style="{StaticResource ReadonlySelectableTextBox}" Text="{Binding File.Name}" FontWeight="SemiBold" />
                            <Image Source="{StaticResource EditDrawingImage}" Margin="14, 0, 0, 0">
                                <Image.Style>
                                    <Style TargetType="{x:Type Image}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=IsEditable}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <ScrollViewer Height="100" Margin="0,20,0,0">
                                <TextBox TextWrapping="Wrap" Width="525">
                                    <TextBox.Style>
                                        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource ReadonlySelectableTextBox}">
                                            <Setter Property="Text" Value="{Binding File.Description}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding File.Description}" Value="{x:Null}">
                                                    <Setter Property="Text" Value="The file has no description." />
                                                    <Setter Property="Foreground" Value="{DynamicResource LightGray2Brush}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding File.Description}" Value="">
                                                    <Setter Property="Text" Value="The file has no description." />
                                                    <Setter Property="Foreground" Value="{DynamicResource LightGray2Brush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                            </ScrollViewer>
                            <Image Source="{StaticResource EditDrawingImage}" Margin="5, 0, 0, 0">
                                <Image.Style>
                                    <Style TargetType="{x:Type Image}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=IsEditable}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                        </StackPanel>
                        <Separator Style="{StaticResource StandardSeparatorStyle}" Width="570" HorizontalAlignment="Left" Margin="0, 20, 0, 0" />
                        <Grid Width="500" HorizontalAlignment="Left" Margin="0, 20, 0, 0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBox Style="{StaticResource ReadonlySelectableTextBox}" Text="FileType" />
                                <TextBox Style="{StaticResource ReadonlySelectableTextBox}" Text="{Binding File.Format}" FontWeight="Medium" Margin="0, 28, 0, 0" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBox Style="{StaticResource ReadonlySelectableTextBox}" Text="Date added" />
                                <TextBox Style="{StaticResource ReadonlySelectableTextBox}" Text="{Binding File.Date, StringFormat=d}" FontWeight="Medium" Margin="0, 28, 0, 0" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBox Style="{StaticResource ReadonlySelectableTextBox}" Text="Size" />
                                <TextBox Style="{StaticResource ReadonlySelectableTextBox}" Text="{Binding File.Size, Converter={StaticResource BytesToSuffixConverter}}" FontWeight="Medium" Margin="0, 28, 0, 0" />
                            </StackPanel>
                            <StackPanel Grid.Column="3">
                                <TextBox Style="{StaticResource ReadonlySelectableTextBox}" Text="Category" />
                                <TextBox Style="{StaticResource ReadonlySelectableTextBox}" Text="{Binding File.Type}" FontWeight="Medium" Margin="0, 28, 0, 0" />
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBox Style="{StaticResource ReadonlySelectableTextBox}" Text="Shared By" />
                                <TextBox Style="{StaticResource ReadonlySelectableTextBox}" Text="{Binding File.SharedByCount, StringFormat={}{0} Peers, Mode=OneWay}" FontWeight="Medium" Margin="0, 28, 0, 0" />
                            </StackPanel>
                            <StackPanel Grid.Column="5">
                                <TextBox Style="{StaticResource ReadonlySelectableTextBox}" Text="Folder" />
                                <TextBox Style="{StaticResource ReadonlySelectableTextBox}" Text="{Binding File.Folder}" FontWeight="Medium" Margin="0, 28, 0, 0" />
                            </StackPanel>
                        </Grid>

                        <Button Width="174" Height="44" Margin="0, 50, 50, 30" Command="{Binding DownloadCommand}" HorizontalAlignment="Right" Content="{Binding ActionButtonContent, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource AcceptButton}">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="{StaticResource Download_LightDrawingImage}" Width="20" Height="20" />
                                        <ContentPresenter DataContext="{Binding}" Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="15, 0, 0, 0" />
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </views:MvxWindow.ContentTemplate>
</views:MvxWindow>